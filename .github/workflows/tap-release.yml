name: Homebrew
on: [release]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Update formula
      uses: mislav/bump-homebrew-formula-action@v1.4
      env:
        COMMITTER_TOKEN: ${{ secrets.GH_TOKEN }}
      with:
        homebrew-tap: tophat/homebrew-bar
        formula-name: yvm
        download-url: ${{ github.event.assets[0].browser_download_url }}
