#!/usr/bin/env bash
if [[ ${CIRCLE_BRANCH} == greenkeeper* ]]; then
    yarn install
else
    if [[ -n ${CI} ]]; then
        yarn install --frozen-lockfile --ci
    else
        yarn install
    fi
fi
